/* Build defines are passed via the SwiftPM target cSettings (-D flags); this
   file exists only because the vendored .c files #include "config.h". */
#ifndef CALLSCRIBE_SPEEXDSP_CONFIG_H
#define CALLSCRIBE_SPEEXDSP_CONFIG_H
#endif
